<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="kr.co.vida.mapper.CrewMapper">
  
  <!-- 전체 조회 -->
  <select id="selectAll" resultType="crdto">
  		SELECT *
  		FROM crew
  </select>
	<!-- 아이디 중복검사 -->
	<select id="idcheck" resultType="_int">
		select count(*)
  		from crew
  		where crew_id = #{crew_id} 	
	</select>
	<!-- 로그인 -->
	<select id="logincheck" resultType="int">
		select crew_no
  		from crew
  		where crew_id = #{crew_id} and crew_pw = #{crew_pw} 				
	</select>

	  <!-- 회원가입 -->
  	<insert id="insertcrew" parameterType="crdto">
  		insert into crew
  		(
  			crew_no,crew_id,crew_pw,
  			crew_name,crew_email,crew_phone,
  			grade,crew_gender,crew_point
  		)
		values (
		#{crew_no_seq},#{crew_id},#{crew_pw},
		#{crew_name},#{crew_email},#{crew_phone},
		#{grade},#{crew_gender},#{crew_point}
		) 
	</insert>
  </mapper>
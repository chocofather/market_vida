<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="kr.co.vida.dto">
  	<select id="selectAll" resultType="cdto">
  		SELECT coupon_no , coupon_name , coupon_dc , coupon_period , coupon_detail
  		FROM coupon
  	</select>
  	
  	<select id="selectOne" parameterType="int" resultType="cdto">
  		SELECT coupon_no , coupon_name , coupon_dc , coupon_period , coupon_detail
  		FROM coupon
  		WHERE coupon_no = #{coupon_no}
  	</select>
  	
  	<insert id="insertOne" parameterType="cdto">
  		INSERT INTO coupon
  		VALUES (#{coupon_no} , #{coupon_name} , #{coupon_dc} , #{coupon_period} , #{coupon_detail})
  	</insert>
  
  	<delete id="deleteOne" parameterType="int">
  		DELETE FROM coupon
  		WHERE coupon_no = #{coupon_no}
  	</delete>
  
  </mapper>